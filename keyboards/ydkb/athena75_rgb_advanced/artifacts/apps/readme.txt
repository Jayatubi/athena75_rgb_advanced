Committed .app packages for host_tool `app update` (except ACE — build locally).

    bash src/app/tools/build_app.sh <name>

Names: life, matrix, maze, settings, brick (in git). ACE: local PNGs in src/app/ace/keyframes/ → build_app.sh ace → artifacts/apps/ace.app (not in git).
