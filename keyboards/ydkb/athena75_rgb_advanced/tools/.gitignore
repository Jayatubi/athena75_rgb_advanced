# Local animation UF2 outputs — never commit or publish.
# PNG/emoji sources: tools/emojis/ (local only, not in git).
*.uf2
penta_kill*.uf2
emojis/

# host_tool: sources in src/host/, binaries in artifacts/host/ — not under tools/
host/
